[pytest]
markers =
    cpu_smoke: fast CPU-only smoke coverage for contracts and lightweight training paths
    gpu_smoke: CUDA-backed smoke coverage for core differentiable paths
    runtime_preflight: launch/entrypoint checks that validate runtime wiring rather than algorithm quality
